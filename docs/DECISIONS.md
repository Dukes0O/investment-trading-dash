# Decisions

Append-only. Format: `YYYY-MM-DD | decision | reason`.

2026-07-22 | Treat the work-index `AGENTS.md` as the governing entry point until this repo-local guide is created in M1 | The kickoff required a file that did not yet exist, while M1 explicitly requires creating it.
2026-07-22 | Keep the pre-registered 30-week baseline identical to Node: long/flat; enter after completed weekly close > 30-week SMA and 10-week SMA > 30-week SMA; exit after completed weekly close < 30-week SMA; execute next daily open | This is the already-implemented baseline named by the protocol; adding a stop would silently change it.
2026-07-22 | Build protective-stop execution and risk sizing as generic engine capabilities, but do not enable a protective stop in the parity baseline | M1 requires intrabar gap-aware stops and live actions require protection, while baseline verification requires exact Node equivalence.
2026-07-22 | Use 5 bps slippage per side and zero explicit commission as the M1 parity default, configurable as separate fields | The Node Strategy Lab applies a combined 5 bps per side; separating fields preserves parity while meeting the fees/slippage requirement.
2026-07-22 | Use completed ISO Monday-start weeks and expose a completed week only on the next trading session | This exactly matches Node `toWeekly` and prevents partial-week leakage.
2026-07-22 | Default walk-forward protocol to 8 calendar years train, 1 calendar year test, 5 trading-session embargo, 1-year step, with the newest 18 months excluded | The spec requires configurable embargoed folds and a holdout but does not state M1 lengths; these defaults yield multiple regimes and remain configuration, not strategy parameters.
2026-07-22 | Ingest the full available history but hard-cap all M1 research and Node parity data at the holdout cutoff | The protocol distinguishes data possession from research exposure and says the newest 18 months remain untouched until promotion.
2026-07-22 | Require `TIINGO_API_TOKEN` (accept legacy alias `TIINGO_API_KEY`) for live fetches; offline weekly runs may use only previously validated immutable snapshots | The spec names Tiingo first and says data failure must halt, so there is no demo or silent provider fallback.
2026-07-22 | Define a separate versioned engine-actions document at `data/reports/engine-actions-<date>.json` and attach it to the existing dossier under `engineActions` | The existing schema is an LLM arbitration report and cannot truthfully be emitted by a deterministic engine; this extends the contract without replacing or fabricating LLM fields.
2026-07-22 | Use a 3× ATR(14) protective stop for weekly action sizing, while leaving the baseline backtest stop-free | The protocol requires protected actions but does not prescribe the M1 stop distance; 3× ATR is explicit, configurable, and does not alter baseline evidence.
2026-07-22 | Default risk to 0.25% of $50,000 closed equity and cap aggregate heat at 1.0% | These are the conservative ends of the non-negotiable budget ranges; Kyle must approve changes.
2026-07-22 | Verify Node parity on identical adjusted OHLC with distributions disabled, while production Python P&L uses raw prices, splits, and cash distributions | Node has only one OHLC series and no distribution ledger; isolating shared signal/execution math makes the cross-check exact without weakening the production accounting model.
2026-07-22 | Model the M1 cash baseline as constant nominal value with no interest | The protocol requires a cash comparator but specifies no risk-free-rate source; adding an unstated yield series would introduce another data decision.
2026-07-22 | Read run provenance from `TRENDDESK_AGENT_MODEL` and `TRENDDESK_AGENT_ROLE`, defaulting to `unspecified` and `quant-implementation`, and always capture the current git commit | Automated runs cannot reliably infer the calling frontier model, but the governance record must never fabricate it.
2026-07-22 | Create one cost-ledger row per weekly date; preserve unknown human hours/data spend as null and mark the row incomplete | Null is auditable missing data; zero would falsely assert no effort or spend.
2026-07-22 | Reconcile vendors with a 0.5% per-bar tolerance, permit isolated outliers only when they are at most 0.1% of overlap and below a 1.0% hard limit, and halt on broader or larger discrepancies | A max-only gate halted on one GLD vendor correction from 2007 (1 of 5,005 rows at 0.509968%; next-largest 0.230021%), which is review evidence but not a corrupt series.
2026-07-22 | Start every walk-forward test fold in cash, using earlier bars only to warm indicators | Carrying a training-period position into the test window made trade counts and trade returns include in-sample history even though equity metrics were normalized at the test boundary.
2026-07-22 | Material runs record `HEAD`, dirty-tree status, and a deterministic source hash | M1 was verified before commit; `HEAD` alone would falsely imply the committed tree fully identifies the executed implementation.
2026-07-22 | Expose a completed week from its own final session's close in both engines (Python `completed_week_features`, Node `trend-30w` prepare) instead of the following session | Review found weekend/Friday runs acted on the week before last; the protocol requires deciding on the just-completed Friday close and filling at the next open. Both engines changed identically so parity remains exact; the prior baseline registry line is superseded, not edited away.
2026-07-22 | `Trend30Week.latest` takes the decision date and applies a calendar completeness test (a week is complete when its Friday session has closed or the calendar week has ended) | The backtest feature array cannot distinguish a partial final week from a holiday-shortened one on the dataset's last row; the live path must never act on a partial week, and a holiday-shortened week is conservatively treated as incomplete until the weekend.
2026-07-22 | Dashboard `analyzeSymbol` drops the trailing weekly bar only when it is genuinely partial (its latest session is not a Friday) | It previously always dropped the last weekly bar, so weekend dossiers scored the rule engine on week-old data — the same defect as the engine's, fixed with the same convention.
2026-07-22 | Weekly-run provenance manifests are written to committed `engine/registry/manifests/`, matching backtest manifests; only HTML output stays local | Runs that drive live orders are registry memory; gitignored provenance contradicted the no-critical-context-outside-the-repo rule.
2026-07-22 | Commit raw Parquet snapshots and their hash manifests (`engine/data/raw/` un-ignored); only the rebuildable DuckDB store stays local | Reproducibility from immutable raw data must survive hardware loss; three symbols of daily history are small enough to version.
2026-07-22 | Cost ledger uses last-row-per-week-wins: re-running with previously missing hours/spend appends a merged corrected row; complete rows are never re-opened | The first implementation silently ignored corrections, permanently freezing incomplete rows; appending corrections preserves the audit trail without editing history.
2026-07-22 | KNOWN LIMITATION (fix before raising risk above 0.25%/trade or adding symbols): position sizing passes `current_heat=0`, so open-position risk does not count against the 1% portfolio heat cap | At 0.25% x 3 symbols the cap cannot be breached, but the heat computation must aggregate live open risk before the risk budget grows.
2026-07-22 | End the M2 operational replay on the latest Friday at or before each symbol's pre-holdout cutoff, and never generate replay decisions from the reserved 18-month holdout | The work order calls replay an operational drill over full history but also says not to touch the holdout; using all available pre-cutoff history for each truncated weekly run satisfies the drill without exposing reserved outcomes.
2026-07-22 | Keep the symmetric `trend-30w-ls-v1` experiment stop-free while extending the simulator with optional long and short protective stops | The registered mirror must add zero strategy parameters and leave the long side unchanged; enabling ATR stops would alter both rules, while generic stop support still satisfies the M2 execution-engine requirement.
2026-07-22 | Accrue short borrow at a configurable 25 bps/year over calendar days on the prior close's short notional, using 365 days/year, and debit cash distributions owed by shorts | The work order specifies the annual default but not the accrual convention; calendar-day accrual captures weekends without adding a trading-calendar parameter, and short sellers economically owe distributions.
2026-07-22 | Halt weekly action generation when an in-scope held position lacks a positive quantity, current price, or active protective stop; ignore portfolio positions outside the configured M2 asset universe | Missing risk inputs cannot truthfully be treated as zero, while unrelated dashboard holdings have no engine action/stop contract in M2.
2026-07-22 | Allocate portfolio heat to new BUY actions sequentially in configured asset order after accounting for all existing held-position risk | The work order requires aggregate heat and multiple-BUY enforcement but does not prescribe allocation priority; configured order is deterministic, reviewable, and adds no research parameter.
2026-07-22 | Leave the incomplete 2026-07-22 cost record unchanged until Kyle supplies actual human hours and data spend | The work order explicitly conditions backfill on Kyle's figures; guessing zero would corrupt the append-only audit trail.
2026-07-22 | Reject `trend-30w-ls-v1` before holdout evaluation and do not promote or add a faster weakening detector in M2 | Across 52 pre-holdout asset-folds it beat long/flat return in 14 and Sharpe in 15; short contribution was positive only for TLT (+18.10 percentage points summed across folds), not GLD (-5.51) or SPY (-26.90), so the result fails the majority-of-folds and multi-asset promotion gate.
2026-07-22 | Count all three identical `trend-30w-ls-v1` executions in trial history; the first two remain registered as `recorded` and the final trial carries the explicit `rejected` verdict | The strategy, data, folds, costs, and metrics were identical; two implementation-validation reruns occurred before verdict emission was corrected, and append-only trial accounting must not hide them.
2026-07-22 | Bump engine-actions to schema v2 and separate `activeStop` from `proposedStop`; current heat uses only the broker-confirmed active stop | A recomputed 3x ATR proposal is not a working order until Kyle approves it. An optional `activeStop` on an in-scope `portfolio.json` position is the authoritative confirmation and overrides the prior action document; without confirmation, live runs carry the prior active stop forward and never auto-promote the proposal. Replay explicitly simulates approval between weeks.
2026-07-22 | Preserve pre-sizing `signalIntent` and emit `DEFER` when a valid BUY has zero heat capacity | Reusing `AVOID` made a portfolio-capacity constraint look bearish and caused replay to misclassify sizing as a signal regression; schema v2 lets consumers distinguish market intent from executable capacity.
2026-07-22 | Replay every eligible calendar Friday, including holiday-shortened weeks whose last session was Thursday, and evaluate the oracle on the prior calendar-complete week in that case | The live completeness rule conservatively waits until the weekend after an early close; replay must exercise and record that one-week lag rather than silently skip the edge case.
2026-07-22 | Implement M3 decline avoidance as `(strategy return - buy-and-hold return) / abs(buy-and-hold return)` | The work order's printed operands produce -100% for a strategy that is flat through the decline, contradicting its explicit definition that flat-through-decline equals +100%; the stated semantic meaning governs.
2026-07-22 | Include the trough and recovery sessions when measuring first-flat and first-long delays | A fill on the trough or recovery session is measurable participation in that phase; inclusive endpoints also make the five episode diagnostics complete for short synthetic cycles.
2026-07-22 | Choose the M3 binding constraint by comparing `max(0, 100 - mean decline avoided)`, `max(0, 100 - mean recovery captured)`, and cumulative whipsaw cost, with all inputs and formulas stored in the manifest | The work order requires the largest measured cost but does not prescribe comparable arithmetic; this makes the interpretation deterministic and auditable without changing strategy parameters.
2026-07-22 | Keep historical replay manifests immutable, but store future full weekly action documents as one minified JSONL sidecar per replay and keep only summaries, line references, and SHA-256 hashes in the manifest | The existing full-year manifests total about 942 KB and 30,205 lines; sidecars preserve complete evidence while compact manifests prevent routine replay diffs from bloating registry history.
